query ErrorPageLayout($slug: String!) {
  errorPage(where: { slug: $slug }) {
    id
    slug
    title
    pageDescription {
      description
    }
    errorPageLayout {
      ...RowHeaderNavigation
      ...ContentBlock
      ...Footer
    }
  }
}
